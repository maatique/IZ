name: Create Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: sync HTML
      run:  rsync -avz /var/www/html/index.nginx-debian.html
   
    - name: Restart Nginx
      run:  sudo systemctl restart nginx
